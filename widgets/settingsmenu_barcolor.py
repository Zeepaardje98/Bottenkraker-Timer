import tkinter as tk
import re

class BarColor:
    def __init__(self, parent, window, settings):
        self.window = window
        self.settings = settings
        self.parent = parent

    def setup_window():
        a = 5
